[project]
name = "Synthetic Data Tools"
description = "Data Synthesizer"
readme = "README.rst"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 100
